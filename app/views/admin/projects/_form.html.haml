= f.input :name
= f.input :description
= f.input :link
= f.input :source_link
= f.input :image
- if @tags.any?
  = f.association :tags, collection: @tags, input_html: { multiple: true }
= render 'layouts/form_actions', f: f
