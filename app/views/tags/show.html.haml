- content_for :title do
  = @tag.name

#tag
  - if @tag.certifications.any?
    %h2 Certifications
    %ul= render @tag.certifications
  - if @tag.educations.any?
    %h2 Education
    %ul= render @tag.educations
  - if @tag.occupations.any?
    %h2 Occupations
    %ul= render @tag.occupations
  - if @tag.projects.any?
    %h2 Projects
    %ul= render @tag.projects
  %p= link_to 'Back', tags_path
