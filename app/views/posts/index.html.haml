- content_for :title do
  Blog

#blog
  %ul
    - @posts.each do |post|
      %li
        = link_to truncate(post.title, length: 50),
          post_path(post.slug), title: post.title
        .date= post.created_at.strftime('%Y-%m-%d %H:%M %Z')

